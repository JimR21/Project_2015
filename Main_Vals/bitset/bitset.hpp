
void bf_array_update(int, char*);	// updates a specifix index bit of the bf array
void printbincharpad(char);			// Prints the bits of a character (DEBUG USE)
int bf_array_check(int, char*);		// checks if index bit is 1 or 0

